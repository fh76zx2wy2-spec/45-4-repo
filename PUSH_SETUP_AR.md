# إشعارات 45/4 — Web Push الحقيقي

الإشعارات في هذا التحديث ليست تنبيهًا محليًا داخل الصفحة؛ الإرسال يتم من Supabase Edge Function إلى جهاز المشترك حتى لو كان 45/4 مغلقًا.

## الإشعارات
- زياد يصل للنادي → عبدالسلام: «زياد وصل النادي الآن 🔥 شد حيلك».
- عبدالسلام يصل → زياد: «عبدالسلام وصل النادي الآن 🏋️‍♂️».
- الخروج → إشعار للرفيق مع مدة الزيارة.
- إكمال 4 أيام حضور مختلفة من الأحد إلى السبت → إشعار 4/4 للرفيق.
- التشجيعات اليدوية → Push مباشر.
- زيارة مفتوحة: تنبيه ذاتي عند 45 دقيقة ثم عند 60 دقيقة.
- زر «أرسل لي إشعارًا تجريبيًا حقيقيًا» في الإعدادات.

## المطلوب في Supabase
1. شغّل `SUPABASE_UPDATE_20260926_FINAL.sql` في SQL Editor.
2. انشر Edge Function من `supabase/functions/push-45-4/index.ts` باسم `push-45-4`.
3. أضف الأسرار: `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`, `CRON_SECRET`. لا تضع المفتاح الخاص أو CRON_SECRET في GitHub.
4. لتشغيل تنبيهي 45 و60 دقيقة والموقع مغلق، شغّل مهمة Cron تستدعي `{"action":"sweep"}` كل 5 دقائق مع الهيدر `x-cron-secret`.
5. كل مستخدم يفتح 45/4 من أيقونة الشاشة الرئيسية ثم يضغط «تفعيل إشعارات 45/4».

> Live Activity / Widget بعدّاد حي على شاشة القفل يحتاج تطبيق iOS أصلي (ActivityKit/WidgetKit)، لذلك ليس جزءًا من نسخة PWA الحالية. العداد نفسه يستمر زمنيًا من وقت الوصول ويصح عند إعادة فتح الموقع.
