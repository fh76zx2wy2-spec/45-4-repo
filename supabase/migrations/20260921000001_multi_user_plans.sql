-- ============================================================================
-- 45/4 — السماح بحساب ثانٍ مع بقاء عزل البيانات لكل مستخدم عبر سياسات own_rows.
-- لا يحذف أي بيانات ولا يغيّر معرّفات المستخدمين الحاليين.
-- ============================================================================

insert into public.allowed_emails (email)
values
  ('z062496@gmail.com'),
  ('amk157662@gmail.com')
on conflict (email) do nothing;

comment on table public.allowed_emails is
  'حسابات Google المصرّح لها بالدخول إلى 45/4. بيانات كل مستخدم معزولة بسياسات RLS حسب auth.uid().';
